"""Content for part3 (STUB — filled in later tasks)."""

L08_ZH = """<p class="lead">（占位）本课「Agent 内部」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L08_EN = """<p class="lead">(stub) Lesson "Agent Internals" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L09_ZH = """<p class="lead">（占位）本课「ChatClient 内部」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L09_EN = """<p class="lead">(stub) Lesson "ChatClient Internals" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L10_ZH = """<p class="lead">（占位）本课「工具调用内部」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L10_EN = """<p class="lead">(stub) Lesson "Tool-Calling Internals" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L11_ZH = """<p class="lead">（占位）本课「中间件 Middleware」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L11_EN = """<p class="lead">(stub) Lesson "Middleware" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L12_ZH = """<p class="lead">（占位）本课「Workflows 工作流引擎」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L12_EN = """<p class="lead">(stub) Lesson "Workflows Engine" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L13_ZH = """<p class="lead">（占位）本课「编排模式」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L13_EN = """<p class="lead">(stub) Lesson "Orchestration Patterns" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""

L14_ZH = """<p class="lead">（占位）本课「流式与可观测性」内容将于后续任务补全。</p>
<div class="card key"><div class="tag">✅ 关键要点</div>待补充。</div>"""

L14_EN = """<p class="lead">(stub) Lesson "Streaming & Observability" — content coming in a later task.</p>
<div class="card key"><div class="tag">✅ Key points</div>TBD.</div>"""
